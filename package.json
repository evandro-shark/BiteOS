{
  "name": "biteos",
  "version": "1.0.0",
  "description": "Sistema Operacional Completo para Lanchonetes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-all": "npm install && cd apps/customer && npm install && cd ../pos && npm install && cd ../kitchen && npm install && cd ../delivery && npm install && cd ../admin && npm install && cd ../management && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}